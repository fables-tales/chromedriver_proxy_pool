require 'spec_helper'

describe ChromedriverProxyPool do
  it 'has a version number' do
    expect(ChromedriverProxyPool::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
